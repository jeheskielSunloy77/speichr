# Example environment variables for Speichr (do NOT commit secrets)
# Copy this file to .env and update values for your environment.

# Secret store used by the app. Options:
# - keytar (use OS keychain integration)
# - memory (in-memory store, useful for CI/tests)
SECRET_STORE=keytar

# When developing with Vite dev server you can point the main process
# at the dev server URL so the Electron window loads the dev bundle.
# Example: http://localhost:5173
MAIN_WINDOW_VITE_DEV_SERVER_URL=

# The renderer build output folder name. Keep as-is unless you changed
# the Vite build target name for the main window.
MAIN_WINDOW_VITE_NAME=main_window

# Optional: enable debug logging (NODE/ELECTRON/third-party libraries)
#DEBUG=
