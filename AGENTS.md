# AI Rules

- NEVER USE ANY SKILL IF NOT EXPLICITLY INSTRUCTED TO DO SO.

for the shadcn/ui refer to the components.json which is the config file for the shadcn/ui components, if you want to tweek the design or move the directory to better suit the project structure you can do that but make sure to update the components.json file with the correct path to the components. use the MCP server to search for components.
you can freely change where to store the components, utils css files or any other files but make sure to update the components.json file with the correct path to the components.
